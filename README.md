# browserstack-product-case
Case submission for Product for BrowserStack

Install node and start the backend server with `node server.js` inside `ai-roadmap-backend` folder

The frontend code rests in the `ai-roadmap-new-frontend` folder

Set an OPENAI_API_KEY variable after creating an API key with OpenAI to ensure that the backend connects to OPEN AI

(node modules folders excluded in upload)
